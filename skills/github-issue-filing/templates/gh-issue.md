# <Short title>

**Type:** <bug | enhancement | documentation | tooling | other>
**Reference:** <docs/SPEC.md §<section>, a design doc, or a related issue — omit this line entirely if not applicable>

## Context

<!-- Where this came from: a docs/ISSUES.md entry, a user conversation, a UAT pass,
     an audit finding. Link or quote the originating discussion if one exists, so
     a reader with zero session context can trace why this exists. -->

## Requirements

<!-- Specific, checkable statements of what must be true when this is done —
     reference the exact rule or spec being implemented, or the exact defect
     being fixed, not a paraphrase. -->

-

## Definition of Done

<!-- What "this works" looks like end to end, concretely — a scenario someone
     could actually walk through, not just "tests pass." -->

-
- A `documentation-sync` pass has run against every commit in this issue's branch
  (stale, invalid, or missing doc coverage) — see the `documentation-sync` skill.

## Out of Scope

<!-- What this issue deliberately does not cover, especially anything adjacent
     that might look like it should be included. -->

-

## Validation

<!-- The exact commands (tests, lint, manual steps) that must pass before this
     issue can close. -->

-

## Checklist

<!-- One checkbox per Requirement/Definition-of-Done item. Check items off as
     work proceeds (edit the issue body), not in one batch at the end. -->

- [ ]
- [ ] `documentation-sync` pass completed (Definition of Done)
